<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Update Category page</title>
  </head>
  <body>
    <%- include('navbar') %>

    <div class="container" style="margin-top: 10%">
      <h2>Update Category</h2>
      <div class="container" style=" display: flex; justify-content: center;">

        <form
          action="/category/update-category"
          method="POST"
          class="update-form"
        >
          <input type="hidden" name="categoryId" value="<%= category.c_id %>" />

          <label for="productName" class="btn" style="border: 2px solid grey; border-radius: 10px; color: grey;">Category Name</label>
          <input
            type="text"
            id="categoryName"
            name="categoryName"
            placeholder="Enter category name"
            value="<%= category.c_name %>"
          />
          <button type="submit" class="btn-submit btn" style=" background-color: cadetblue; color: aliceblue; margin-top: 2%;">Update Category</button>
        </form>
      </div>
    </div>
  </body>
</html>
